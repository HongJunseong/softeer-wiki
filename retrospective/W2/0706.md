# 2026-07-06 회고

## Keep
- `multiprocessing`의 `Process`, `Queue`, `Pool`을 활용하여 여러 작업을 병렬로 처리하는 방법을 익혔다.
- Git 브랜치를 `main` 기준으로 생성하고 관리하는 방법을 다시 정리할 수 있었다.

## Problem
- `Queue.get_nowait()`와 예외 처리, 프로세스 간 작업 분배 방식을 이해하는 데 시간이 걸렸다.
- 브랜치를 잘못 생성해 구조가 꼬였고, 이를 수정하는 과정에서 Git 사용에 미숙한 부분을 느꼈다.

## Try
- `multiprocessing` 관련 예제를 직접 다시 작성하며 개념을 복습하겠다.
- 앞으로는 항상 `main`에서 최신 내용을 가져온 뒤 새로운 브랜치를 생성하는 습관을 유지하겠다.